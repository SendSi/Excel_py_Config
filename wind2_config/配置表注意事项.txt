1、配置表导出的lua文件名字以excel名字为准，不在读表格第一行第一列的内容产生导出文件名
2、点击新建表格会新建一张表，并给出基础的表格格式，注意表格最后一行最后一列必须要有“End”，不然导出会出错
3、如果要该表，请务必先点击锁定表格，不然容易造成冲突，点击锁表的时候，会先自动调用一次svn更新
4、点击导出数据，会导出lua文件并上传数据库
5、点击提交数据，会先做一次导出数据，然后上传本表以及lua文件到svn
6、所有lua表格都会统一导出到..\client_branch\LuaScripts\lua_source\config\configlogic
